/*  in terminal  */

//1) average
// test=# SELECT AVG(score) FROM users;
    // avg         
    // ---------------------
    // 82.5000000000000000
    // (1 row)

// 2)sum
// test=# SELECT SUM(age) FROM users;
    // sum 
    // -----
    //  124
    // (1 row)

// 3)count
// test=# SELECT COUNT(name) FROM users;
    // count 
    // -------
    //      4
    // (1 row)

